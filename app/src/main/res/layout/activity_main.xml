<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="gusev.spbstu.org.musicplayer.ui.MainActivity" android:layout_gravity="center">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" android:gravity="center" android:layout_gravity="center"
            android:layout_marginBottom="80dp">

        <TextView
                android:text="@string/tv_where_to_play_music"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:id="@+id/tv_where_get_music" android:textAlignment="center"
                android:textSize="26sp" android:fontFamily="sans-serif-black"
                android:textColor="@color/black"
                android:layout_marginTop="8dp"/>
        <TextView
                android:text="@string/tv_choose_folder_with_audio"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:id="@+id/tv_choose_folder" android:textAlignment="center"
                android:layout_marginTop="8dp" android:textSize="16sp"/>
        <Button
                android:text="@string/btn_choose_folder"
                android:layout_width="wrap_content"
                android:layout_height="40dp" android:id="@+id/btn_open_folder" android:textAlignment="center"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_open_folder_btn"
                android:textColor="@color/colorAccent"
                android:textAllCaps="false"
                android:textSize="18sp"
                android:paddingStart="20dp"
                android:paddingEnd="20dp" android:onClick="onClick"/>
    </LinearLayout>

    <include layout="@layout/music_player"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
